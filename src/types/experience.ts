export type ExperienceType = 'tour' | 'ziyarat';
export interface ExperienceDeparture { id: string; startDate: string; endDate: string; availableMock: number; transport: string; basePrice: number; status: 'available' | 'limited' | 'soon'; }
export interface ExperiencePackage { id: string; name: string; hotel: string; hotelStars: number; roomType: string; mealPlan: string; includedServices: string[]; pricePerAdult: number; pricePerChild: number; pricePerInfant?: number; singleSupplement?: number; currency: 'IRR'; transport?: string; capacityMock?: number; }
export interface ItineraryDay { day: number; title: string; description: string; }
export interface ExperienceOffer { id: string; slug: string; type: ExperienceType; title: string; destination: string; destinations: string[]; origin: string; country: string; durationDays: number; durationNights: number; images: string[]; description: string; highlights: string[]; departureOptions: ExperienceDeparture[]; packages: ExperiencePackage[]; includedServices: string[]; excludedServices: string[]; visaInfo: string; documents: string[]; itinerary: ItineraryDay[]; tags: string[]; featured?: boolean; startingPrice: number; mealPlan?: string; guide?: string; }
export interface ExperienceFilters { destination: string; origin: string; maxPrice: number; minDays: number; maxDays: number; transport: string[]; hotelStars: number[]; visaRequired: 'all' | 'yes' | 'no'; tags: string[]; accommodation: string[]; }
export interface ExperienceTraveler { id: string; firstName: string; lastName: string; firstNameLatin: string; lastNameLatin: string; birthDate: string; nationalId: string; passportNumber: string; ageCategory: 'adult' | 'child' | 'infant'; }
export interface ExperienceBookingDraft { experienceType: ExperienceType; offer: ExperienceOffer; departure: ExperienceDeparture; package: ExperiencePackage; travelers: ExperienceTraveler[]; addOns: string[]; }
