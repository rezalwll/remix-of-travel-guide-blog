import { useEffect, useState } from 'react';
import { CheckCircle2, CircleX, Home, Printer, RefreshCcw, XCircle } from 'lucide-react';
import { Link, useNavigate, useSearchParams } from 'react-router-dom';
import Layout from '@/components/layout/Layout';
import { completeSuccessfulAttempt, createFailedTransaction, getAttempt, getOrder } from '@/services/payment';
import { readBookingDraft } from '@/store/booking';
import type { MockOrder } from '@/types/payment';
import { formatPrice } from '@/utils/flight';

const CheckoutResult = () => { const navigate = useNavigate(); const [query] = useSearchParams(); const state = query.get('state') || 'success'; const attemptId = query.get('attempt') || ''; const [order, setOrder] = useState<MockOrder | null>(() => query.get('order') ? getOrder(query.get('order') || '') : null); const [reference, setReference] = useState(''); const draft = readBookingDraft(); useEffect(() => { if (order || !attemptId) return; const attempt = getAttempt(attemptId); if (!attempt) return; if (state === 'success' && draft) { const result = completeSuccessfulAttempt(draft, attempt); setOrder(result.order); setReference(result.transaction.referenceNumber); navigate(`/checkout/result?order=${result.order.id}&state=success`, { replace: true }); } else if (state === 'failed') { setReference(createFailedTransaction(attempt, attempt.failureReason || 'پرداخت آزمایشی ناموفق بود.').referenceNumber); } }, [attemptId, draft, navigate, order, state]); const isSuccess = state === 'success' && !!order; const title = isSuccess ? 'رزرو آزمایشی ثبت شد' : state === 'cancelled' ? 'پرداخت لغو شد' : 'پرداخت انجام نشد'; const Icon = isSuccess ? CheckCircle2 : state === 'cancelled' ? XCircle : CircleX; const hotel = order?.serviceType === 'hotel' ? order.hotelSnapshot : null; if (state === 'success' && !order && attemptId) return <Layout><main className="container-page flex min-h-[65vh] items-center justify-center"><p className="text-sm text-muted-foreground">در حال ثبت نتیجهٔ پرداخت آزمایشی...</p></main></Layout>; return <Layout><main className="bg-muted/40 py-8"><div className="container-page"><div className="mx-auto max-w-3xl"><div className={`rounded-2xl border bg-card p-6 text-center sm:p-10 ${isSuccess ? 'border-secondary/30' : 'border-destructive/20'}`}><Icon className={`mx-auto size-16 ${isSuccess ? 'text-secondary' : 'text-destructive'}`} /><h1 className="mt-5 text-2xl font-extrabold">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{isSuccess ? 'اطلاعات زیر برای نسخه آزمایشی هستند و تأیید واقعی تامین‌کننده محسوب نمی‌شوند.' : 'اطلاعات رزرو شما حفظ شده و می‌توانید دوباره تلاش کنید.'}</p>{isSuccess && order ? <div className="mt-7 grid gap-3 text-start sm:grid-cols-3"><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">شماره سفارش</p><p className="mt-1 font-extrabold">{order.orderNumber}</p></div><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">پیگیری آزمایشی</p><p className="mt-1 font-extrabold">{order.payment.transactionReference}</p></div><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">مبلغ پرداخت</p><p className="mt-1 font-extrabold text-primary">{formatPrice(order.pricing.total)}</p></div></div> : <div className="mt-6 rounded-xl bg-destructive/5 p-4 text-sm text-destructive">شماره پیگیری نمونه: {reference || 'ایجاد نشد'}</div>}<div className="mt-7 flex flex-wrap justify-center gap-2">{isSuccess && order ? <button type="button" onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-lg border border-border px-4 py-3 text-sm font-bold"><Printer className="size-4" /> چاپ رسید</button> : <Link to="/checkout/payment" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-bold text-white"><RefreshCcw className="size-4" /> تلاش مجدد</Link>}<Link to={isSuccess ? `/orders/${order?.id}` : '/checkout/review'} className="rounded-lg border border-border px-4 py-3 text-sm font-bold">{isSuccess ? 'مشاهده جزئیات سفارش' : 'بازگشت'}</Link><Link to="/" className="inline-flex items-center gap-2 rounded-lg bg-secondary px-4 py-3 text-sm font-bold text-white"><Home className="size-4" /> صفحه اصلی</Link></div></div>{isSuccess && order && <div className="mt-5 rounded-2xl border border-border bg-card p-5 text-start">{hotel ? <><h2 className="font-extrabold">جزئیات اقامت</h2><p className="mt-3 text-sm font-bold">{hotel.hotel.name} · {hotel.hotel.city}</p><p className="mt-2 text-sm">{hotel.stayParams.checkIn} تا {hotel.stayParams.checkOut} · {hotel.nights} شب · {hotel.room.name}</p><p className="mt-2 text-xs text-muted-foreground">{hotel.guests.length} مهمان · {hotel.ratePlan.title}</p></> : <><h2 className="font-extrabold">جزئیات سفر</h2>{[order.outbound, order.inbound].filter(Boolean).map((flight, index) => flight && <p key={flight.id} className="mt-3 text-sm">{index ? 'برگشت' : 'رفت'} · {flight.fromCity} ← {flight.toCity} · {flight.flightNumber}</p>)}</>}</div>}</div></div></main></Layout>; };
export default CheckoutResult;
