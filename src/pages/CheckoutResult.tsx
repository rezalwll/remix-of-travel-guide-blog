import { useEffect, useState } from 'react';
import { CheckCircle2, CircleX, Home, Printer, RefreshCcw, XCircle } from 'lucide-react';
import { Link, useNavigate, useSearchParams } from 'react-router-dom';
import Layout from '@/components/layout/Layout';
import { completeSuccessfulAttempt, createFailedTransaction, getAttempt, getOrder } from '@/services/payment';
import { readBookingDraft } from '@/store/booking';
import type { MockOrder } from '@/types/payment';
import { formatPrice } from '@/utils/flight';

const CheckoutResult=()=>{const navigate=useNavigate();const [query]=useSearchParams();const state=query.get('state')||'success';const attemptId=query.get('attempt')||'';const [order,setOrder]=useState<MockOrder|null>(()=>query.get('order')?getOrder(query.get('order')||''):null);const [reference,setReference]=useState('');const draft=readBookingDraft();useEffect(()=>{if(order||!attemptId)return;const attempt=getAttempt(attemptId);if(!attempt)return;if(state==='success'&&draft){const result=completeSuccessfulAttempt(draft,attempt);setOrder(result.order);setReference(result.transaction.referenceNumber);navigate(`/checkout/result?order=${result.order.id}&state=success`,{replace:true});}else if(state==='failed')setReference(createFailedTransaction(attempt,attempt.failureReason||'پرداخت آزمایشی ناموفق بود.').referenceNumber);},[attemptId,draft,navigate,order,state]);const success=state==='success'&&!!order;const Icon=success?CheckCircle2:state==='cancelled'?XCircle:CircleX;const exp=order?.experienceSnapshot;const hotel=order?.hotelSnapshot; if(state==='success'&&!order&&attemptId)return <Layout><main className="container-page flex min-h-[65vh] items-center justify-center">در حال ثبت نتیجه پرداخت آزمایشی...</main></Layout>;return <Layout><main className="bg-muted/40 py-8"><div className="container-page max-w-3xl"><section className="rounded-2xl border border-border bg-card p-6 text-center sm:p-10"><Icon className={`mx-auto size-16 ${success?'text-secondary':'text-destructive'}`}/><h1 className="mt-5 text-2xl font-extrabold">{success?(exp?`رزرو آزمایشی ${exp.type==='tour'?'تور':'زیارت'} ثبت شد`:'رزرو آزمایشی ثبت شد'):state==='cancelled'?'پرداخت لغو شد':'پرداخت انجام نشد'}</h1><p className="mt-2 text-sm text-muted-foreground">این صفحه نمونه است و صدور وچر، ویزا یا تایید رسمی را تضمین نمی‌کند.</p>{success&&order?<div className="mt-7 grid gap-3 text-start sm:grid-cols-3"><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">شماره سفارش</p><p className="mt-1 font-extrabold">{order.orderNumber}</p></div><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">پیگیری نمونه</p><p className="mt-1 font-extrabold">{order.payment.transactionReference}</p></div><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs text-muted-foreground">مبلغ</p><p className="mt-1 font-extrabold text-primary">{formatPrice(order.pricing.total)}</p></div></div>:<p className="mt-6 rounded-xl bg-destructive/5 p-4 text-sm text-destructive">پیگیری نمونه: {reference||'ایجاد نشد'}</p>}<div className="mt-7 flex flex-wrap justify-center gap-2">{success&&order?<button type="button" onClick={()=>window.print()} className="inline-flex items-center gap-2 rounded-lg border border-border px-4 py-3 text-sm font-bold"><Printer className="size-4"/> چاپ</button>:<Link to="/checkout/payment" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-bold text-white"><RefreshCcw className="size-4"/> تلاش مجدد</Link>}<Link to={success?`/orders/${order?.id}`:'/'} className="rounded-lg border border-border px-4 py-3 text-sm font-bold">{success?'جزئیات سفارش':'بازگشت'}</Link><Link to="/" className="inline-flex items-center gap-2 rounded-lg bg-secondary px-4 py-3 text-sm font-bold text-white"><Home className="size-4"/> خانه</Link></div></section>{success&&order&&<section className="mt-5 rounded-2xl border border-border bg-card p-5">{exp?<><h2 className="font-extrabold">{exp.offer.title}</h2><p className="mt-3 text-sm">{exp.offer.destinations.join('، ')} · {exp.departure.startDate} تا {exp.departure.endDate}</p><p className="mt-2 text-sm">{exp.package.name} · {exp.package.hotel} · {exp.departure.transport}</p><p className="mt-3 text-xs text-muted-foreground">{exp.travelers.length} مسافر · داده‌های برنامه و ظرفیت نمایشی هستند.</p></>:hotel?<><h2 className="font-extrabold">{hotel.hotel.name}</h2><p className="mt-3 text-sm">{hotel.stayParams.checkIn} تا {hotel.stayParams.checkOut} · {hotel.nights} شب · {hotel.room.name}</p></>:<><h2 className="font-extrabold">جزئیات سفر</h2>{[order.outbound,order.inbound].filter(Boolean).map((flight,index)=>flight&&<p key={flight.id} className="mt-3 text-sm">{index?'برگشت':'رفت'} · {flight.fromCity} ← {flight.toCity}</p>)}</>}</section>}</div></main></Layout>};
export default CheckoutResult;
