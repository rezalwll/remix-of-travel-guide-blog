import { useState } from 'react';
import { Check, ChevronLeft, Edit3, TicketPercent } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import Layout from '@/components/layout/Layout';
import { CheckoutStepper, EmptyCheckout, FlightSummary, PriceSummary } from '@/components/checkout/CheckoutShared';
import { calculatePrice, validateCoupon } from '@/services/checkout';
import { readBookingDraft, writeBookingDraft } from '@/store/booking';
import { formatPrice } from '@/utils/flight';

const CheckoutReview = () => { const navigate = useNavigate(); const [draft, setDraft] = useState(() => readBookingDraft()); const [couponCode, setCouponCode] = useState(''); const [couponMessage, setCouponMessage] = useState(''); const [couponError, setCouponError] = useState(false); if (!draft?.outbound) return <Layout><EmptyCheckout /></Layout>; const update = (next: Partial<typeof draft>) => { const value = { ...draft, ...next }; setDraft(value); writeBookingDraft(value); }; const applyCoupon = () => { const current = calculatePrice(draft.outbound, draft.inbound, draft.passengers, draft.ancillaries, null); const result = validateCoupon(couponCode, current.total); if (!result) { setCouponError(true); setCouponMessage('کد تخفیف معتبر نیست یا حداقل مبلغ رعایت نشده است.'); return; } setCouponError(false); setCouponMessage(result.message); update({ coupon: result }); }; const price = calculatePrice(draft.outbound, draft.inbound, draft.passengers, draft.ancillaries, draft.coupon); return <Layout><main className="bg-muted/40"><div className="container-page py-5 pb-28 sm:py-8"><CheckoutStepper active={2} /><div className="grid gap-5 lg:grid-cols-[1fr_330px]"><div className="space-y-4"><FlightSummary draft={draft} /><section className="rounded-2xl border border-border bg-card p-4 sm:p-5"><div className="mb-4 flex items-center justify-between"><h2 className="font-extrabold">اطلاعات خریدار</h2><Link to="/checkout/passengers#buyer" className="inline-flex items-center gap-1 text-xs font-bold text-secondary"><Edit3 className="size-3.5" /> ویرایش</Link></div><div className="grid gap-2 text-sm sm:grid-cols-2"><p><span className="text-muted-foreground">نام:</span> {draft.buyer.firstName} {draft.buyer.lastName}</p><p><span className="text-muted-foreground">موبایل:</span> {draft.buyer.mobile}</p><p className="sm:col-span-2"><span className="text-muted-foreground">ایمیل:</span> {draft.buyer.email}</p></div></section><section className="rounded-2xl border border-border bg-card p-4 sm:p-5"><div className="mb-4 flex items-center justify-between"><h2 className="font-extrabold">مسافران ({draft.passengers.length})</h2><Link to="/checkout/passengers" className="inline-flex items-center gap-1 text-xs font-bold text-secondary"><Edit3 className="size-3.5" /> ویرایش اطلاعات</Link></div><div className="space-y-3">{draft.passengers.map((passenger, index) => <div key={passenger.id} className="flex items-center justify-between rounded-lg bg-muted/60 p-3 text-sm"><div><p className="font-bold">{index + 1}. {passenger.firstNameFa} {passenger.lastNameFa}</p><p className="mt-1 text-xs text-muted-foreground">{passenger.firstNameLatin} {passenger.lastNameLatin} · {passenger.type === 'adult' ? 'بزرگسال' : passenger.type === 'child' ? 'کودک' : 'نوزاد'}</p></div><span className="text-xs text-muted-foreground">{passenger.passportNumber ? `${passenger.passportNumber.slice(0, 3)}••••${passenger.passportNumber.slice(-2)}` : passenger.nationalId ? `${passenger.nationalId.slice(0, 2)}••••••${passenger.nationalId.slice(-2)}` : 'اطلاعات ناقص'}</span></div>)}</div></section>{draft.ancillaries.length > 0 && <section className="rounded-2xl border border-border bg-card p-4 sm:p-5"><div className="mb-3 flex items-center justify-between"><h2 className="font-extrabold">خدمات انتخابی</h2><Link to="/checkout/passengers" className="text-xs font-bold text-secondary">ویرایش</Link></div>{draft.ancillaries.map((id) => <p key={id} className="flex justify-between py-1 text-sm"><span>{id.includes('insurance') ? 'بیمه مسافرتی' : 'ترانسفر فرودگاهی'}</span><span>انتخاب شده</span></p>)}</section>}<section className="rounded-2xl border border-border bg-card p-4 sm:p-5"><div className="mb-3 flex items-center gap-2"><TicketPercent className="size-5 text-secondary" /><h2 className="font-extrabold">کد تخفیف</h2></div><div className="flex gap-2"><input value={couponCode} onChange={(e) => setCouponCode(e.target.value)} placeholder="مثلاً TRAVEL500" dir="ltr" className="min-h-11 flex-1 rounded-lg border border-border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring" /><button type="button" onClick={applyCoupon} className="rounded-lg bg-secondary px-4 text-sm font-bold text-white">اعمال</button></div>{couponMessage && <p className={`mt-2 text-xs font-semibold ${couponError ? 'text-destructive' : 'text-secondary'}`}>{couponMessage}</p>}{draft.coupon && <button type="button" onClick={() => { setCouponCode(''); update({ coupon: null }); }} className="mt-2 text-xs font-bold text-primary">حذف کد {draft.coupon.code}</button>}</section><section className="rounded-2xl border border-border bg-card p-4 sm:p-5"><label className="flex items-start gap-3 text-sm leading-6"><input type="checkbox" checked={draft.termsAccepted} onChange={(e) => update({ termsAccepted: e.target.checked })} className="mt-1 size-4 accent-primary" /><span>قوانین رزرو و شرایط استرداد را مطالعه کرده‌ام و می‌پذیرم. <Link to="/terms" className="font-bold text-secondary">مشاهده قوانین نمونه</Link></span></label></section></div><aside className="h-fit space-y-3 lg:sticky lg:top-24"><PriceSummary draft={draft} /><button type="button" disabled={!draft.termsAccepted} onClick={() => navigate('/checkout/payment')} className="hidden min-h-12 w-full items-center justify-center gap-2 rounded-lg bg-primary text-sm font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40 lg:flex">ادامه به پرداخت <ChevronLeft className="size-4" /></button></aside></div><div className="fixed inset-x-0 bottom-0 z-40 border-t border-border bg-card p-3 lg:hidden"><div className="flex items-center justify-between gap-3"><div><p className="text-[11px] text-muted-foreground">مبلغ نهایی</p><p className="font-extrabold text-primary">{formatPrice(price.total)}</p></div><button type="button" disabled={!draft.termsAccepted} onClick={() => navigate('/checkout/payment')} className="rounded-lg bg-primary px-5 py-3 text-sm font-bold text-primary-foreground disabled:opacity-40">ادامه به پرداخت</button></div></div></div></main></Layout>; };
export default CheckoutReview;
