import { useState } from 'react';
import { ChevronLeft, Edit3, TicketPercent } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import Layout from '@/components/layout/Layout';
import { BookingSummary, CheckoutStepper, EmptyCheckout, PriceSummary } from '@/components/checkout/CheckoutShared';
import { calculateAnyDraftPrice, validateCoupon } from '@/services/checkout';
import { readBookingDraft, writeBookingDraft } from '@/store/booking';
import type { BookingDraft, ExperienceCheckoutDraft, HotelBookingDraft, SecondaryCheckoutDraft } from '@/types/checkout';
const CheckoutReview=()=>{const navigate=useNavigate();const [draft,setDraft]=useState<BookingDraft|null>(()=>readBookingDraft());const [coupon,setCoupon]=useState('');const [message,setMessage]=useState('');if(!draft||(!draft.serviceType&&!draft.outbound))return <Layout><EmptyCheckout/></Layout>;const hotel=draft.serviceType==='hotel'?draft as HotelBookingDraft:null;const experience=draft.serviceType==='tour'||draft.serviceType==='ziyarat'?draft as ExperienceCheckoutDraft:null;const secondary=['train','bus','insurance','cip','transfer'].includes(draft.serviceType||'')?draft as SecondaryCheckoutDraft:null;const update=(next:Partial<BookingDraft>)=>{const value={...draft,...next};setDraft(value);writeBookingDraft(value)};const apply=()=>{const result=validateCoupon(coupon,calculateAnyDraftPrice({...draft,coupon:null}).total);if(!result){setMessage('کد تخفیف معتبر نیست.');return;}setMessage(result.message);update({coupon:result})};return <Layout><main className="bg-muted/40"><div className="container-page py-5 pb-28 sm:py-8"><CheckoutStepper active={2} firstLabel={hotel||experience||secondary?'اطلاعات مسافران':undefined}/><div className="grid gap-5 lg:grid-cols-[1fr_330px]"><div className="space-y-4"><BookingSummary draft={draft}/><section className="rounded-2xl border border-border bg-card p-5"><div className="flex justify-between"><h2 className="font-extrabold">اطلاعات خریدار</h2><Link to={hotel?'/checkout/hotel-guests':experience?`/checkout/${experience.experienceType}-travelers`:secondary?'/checkout/secondary-passengers':'/checkout/passengers'} className="inline-flex items-center gap-1 text-xs font-bold text-secondary"><Edit3 className="size-3.5"/> ویرایش</Link></div><div className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><p>نام: {draft.buyer.firstName} {draft.buyer.lastName}</p><p>موبایل: {draft.buyer.mobile}</p><p className="sm:col-span-2">ایمیل: {draft.buyer.email}</p></div></section>{experience?<section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-extrabold">مسافران تور/زیارت</h2>{experience.travelers.map((item)=><p key={item.id} className="mt-2 rounded-lg bg-muted/60 p-3 text-sm">{item.firstName} {item.lastName} · {item.ageCategory}</p>)}</section>:secondary?<section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-extrabold">مسافران سرویس</h2>{secondary.travelers.map((item,index)=><p key={item.id} className="mt-2 rounded-lg bg-muted/60 p-3 text-sm">{index+1}. {item.firstName} {item.lastName}{secondary.selectedSeats?.[index]?` · صندلی ${secondary.selectedSeats[index]}`:''}</p>)}</section>:hotel?<section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-extrabold">مهمانان هتل</h2>{hotel.hotelGuests.map((item)=><p key={item.id} className="mt-2 rounded-lg bg-muted/60 p-3 text-sm">{item.firstName} {item.lastName}</p>)}</section>:<section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-extrabold">مسافران ({draft.passengers.length})</h2>{draft.passengers.map((item)=><p key={item.id} className="mt-2 rounded-lg bg-muted/60 p-3 text-sm">{item.firstNameFa} {item.lastNameFa}</p>)}</section>}<section className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center gap-2"><TicketPercent className="size-5 text-secondary"/><h2 className="font-extrabold">کد تخفیف</h2></div><div className="mt-3 flex gap-2"><input value={coupon} onChange={(e)=>setCoupon(e.target.value)} placeholder="مثلاً TRAVEL500" className="min-h-11 flex-1 rounded-lg border px-3"/><button type="button" onClick={apply} className="rounded-lg bg-secondary px-4 text-sm font-bold text-white">اعمال</button></div>{message&&<p className="mt-2 text-xs text-secondary">{message}</p>}</section><label className="flex gap-3 rounded-2xl border border-border bg-card p-4 text-sm"><input type="checkbox" checked={draft.termsAccepted} onChange={(e)=>update({termsAccepted:e.target.checked})} className="mt-1 size-4 accent-secondary"/> قوانین و شرایط نسخه آزمایشی را می‌پذیرم.</label></div><aside className="h-fit space-y-3 lg:sticky lg:top-24"><PriceSummary draft={draft}/><button type="button" onClick={()=>navigate('/checkout/payment')} disabled={!draft.termsAccepted} className="hidden w-full rounded-lg bg-primary py-3 text-sm font-bold text-white disabled:opacity-40 lg:block">ادامه پرداخت <ChevronLeft className="ms-1 inline size-4"/></button></aside></div><div className="fixed inset-x-0 bottom-0 z-40 border-t border-border bg-card p-3 lg:hidden"><button type="button" onClick={()=>navigate('/checkout/payment')} disabled={!draft.termsAccepted} className="w-full rounded-lg bg-primary py-3 text-sm font-bold text-white disabled:opacity-40">ادامه پرداخت</button></div></div></main></Layout>};export default CheckoutReview;
