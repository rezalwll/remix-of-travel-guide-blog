import { useMemo, useState } from 'react';
import { Search } from 'lucide-react';
import { Link } from 'react-router-dom';
import Layout from '@/components/layout/Layout';
import { getAllArticles } from '@/data/destinations';
export default function BlogIndex(){const [query,setQuery]=useState('');const articles=getAllArticles();const categories=['همه',...new Set(articles.map((item)=>item.category))];const [category,setCategory]=useState('همه');const filtered=useMemo(()=>articles.filter((item)=>(category==='همه'||item.category===category)&&`${item.title} ${item.excerpt}`.toLowerCase().includes(query.toLowerCase())),[articles,category,query]);return <Layout><main className="bg-muted/40 py-8 sm:py-12"><div className="container-page"><section className="rounded-2xl bg-[hsl(220_22%_17%)] p-7 text-white sm:p-10"><p className="text-xs font-bold text-[hsl(42_100%_60%)]">مجله سفر کی‌آشی</p><h1 className="mt-2 text-3xl font-extrabold">قبل از حرکت بخوان</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-white/70">راهنماهای مقصد، ویزا و نکات کاربردی سفر برای تصمیم‌گیری بهتر.</p><div className="relative mt-6 max-w-xl"><Search className="absolute end-3 top-3.5 size-5 text-muted-foreground"/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="جست‌وجو در مجله" className="min-h-12 w-full rounded-xl border-0 bg-white px-4 pe-11 text-sm text-foreground"/></div></section><div className="mt-6 flex flex-wrap gap-2">{categories.map((item)=><button key={item} type="button" onClick={()=>setCategory(item)} className={`rounded-full px-4 py-2 text-xs font-bold ${category===item?'bg-primary text-white':'border border-border bg-card'}`}>{item}</button>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((article)=><Link key={article.id} to={`/article/${article.id}`} className="overflow-hidden rounded-2xl border border-border bg-card transition hover:-translate-y-0.5 hover:border-secondary"><img src={article.image} alt={article.title} loading="lazy" className="aspect-[1.5] w-full object-cover"/><div className="p-4"><p className="text-xs font-bold text-primary">{article.category}</p><h2 className="mt-2 font-extrabold leading-6">{article.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{article.excerpt}</p></div></Link>)}</div></div></main></Layout>}
