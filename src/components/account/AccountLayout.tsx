import { Bell, Heart, LogOut, Plane, RotateCcw, Ticket, UserRound, Users, WalletCards, Headphones } from 'lucide-react';
import { NavLink, useNavigate } from 'react-router-dom';
import { useAuth } from '@/context/AuthContext';
import { cn } from '@/lib/utils';
const links = [{ to: '/account', label: 'داشبورد', icon: Plane }, { to: '/account/orders', label: 'سفارش‌ها', icon: Ticket }, { to: '/account/passengers', label: 'مسافران من', icon: Users }, { to: '/account/wallet', label: 'کیف پول', icon: WalletCards }, { to: '/account/refunds', label: 'درخواست استرداد', icon: RotateCcw }, { to: '/account/favorites', label: 'علاقه‌مندی‌ها', icon: Heart }, { to: '/account/notifications', label: 'اعلان‌ها', icon: Bell }, { to: '/account/support', label: 'پشتیبانی', icon: Headphones }, { to: '/account/profile', label: 'پروفایل', icon: UserRound }];
export const AccountLayout = ({ children }: { children: React.ReactNode }) => { const { user, logout } = useAuth(); const navigate = useNavigate(); return <main className="bg-muted/40 py-5 sm:py-8"><div className="container-page"><div className="mb-5 rounded-2xl border border-border bg-card p-4 sm:p-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-full bg-primary text-lg font-extrabold text-white">{user?.firstName.slice(0, 1)}</span><div><p className="text-xs text-muted-foreground">حساب کاربری</p><h1 className="font-extrabold">سلام {user?.firstName} 👋</h1></div></div></div><div className="grid gap-5 lg:grid-cols-[230px_1fr]"><aside className="hidden h-fit rounded-2xl border border-border bg-card p-2 lg:block">{links.map(({ to, label, icon: Icon }) => <NavLink end={to === '/account'} key={to} to={to} className={({ isActive }) => cn('mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition', isActive ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:bg-muted hover:text-foreground')}><Icon className="size-4" />{label}</NavLink>)}<button type="button" onClick={() => { logout(); navigate('/'); }} className="mt-3 flex w-full items-center gap-3 border-t border-border px-3 py-4 text-sm font-semibold text-destructive"><LogOut className="size-4" /> خروج</button></aside><div><div className="mb-4 flex gap-2 overflow-x-auto rounded-xl border border-border bg-card p-2 lg:hidden">{links.map(({ to, label, icon: Icon }) => <NavLink end={to === '/account'} key={to} to={to} className={({ isActive }) => cn('flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs font-bold', isActive ? 'bg-primary text-white' : 'text-muted-foreground')}><Icon className="size-3.5" />{label}</NavLink>)}</div>{children}</div></div></div></main>; };
