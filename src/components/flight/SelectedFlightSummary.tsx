import { ArrowLeft, Check, Plane } from 'lucide-react';
import type { FlightResult } from '@/types/flight';
import { formatPrice, formatTime } from '@/utils/flight';

const SelectedFlightSummary = ({ outbound, inbound, onContinue }: { outbound: FlightResult | null; inbound: FlightResult | null; onContinue: () => void }) => { if (!outbound && !inbound) return null; const total = (outbound?.price ?? 0) + (inbound?.price ?? 0); return <div className="fixed inset-x-0 bottom-0 z-40 border-t border-border bg-card/95 p-3 shadow-2xl backdrop-blur sm:sticky sm:bottom-4 sm:mx-auto sm:mt-4 sm:max-w-6xl sm:rounded-2xl sm:border"><div className="container-page flex flex-col gap-3 p-0 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap items-center gap-x-5 gap-y-2 text-sm"><span className="inline-flex items-center gap-1.5 font-bold text-secondary"><Check className="size-4" /> {outbound ? 'پرواز رفت انتخاب شد' : 'پرواز را انتخاب کنید'}</span>{outbound && <span className="text-muted-foreground">رفت: {outbound.fromCity} → {outbound.toCity} · {formatTime(outbound.departure)}</span>}{inbound && <span className="text-muted-foreground">برگشت: {inbound.fromCity} → {inbound.toCity} · {formatTime(inbound.departure)}</span>}</div><div className="flex items-center justify-between gap-4"><div className="text-end"><p className="text-[11px] text-muted-foreground">مجموع تقریبی</p><p className="font-extrabold text-primary">{formatPrice(total)}</p></div><button type="button" onClick={onContinue} disabled={!outbound || (!!outbound && !inbound && !!inbound)} className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-45">{inbound ? 'ادامه رزرو' : 'انتخاب برگشت'} <ArrowLeft className="size-4" /></button></div></div></div>; };
export default SelectedFlightSummary;
